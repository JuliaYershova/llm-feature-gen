You are an expert in natural language processing and explainable artificial intelligence. Your task is to analyze textual examples and discover interpretable, binary (Yes/No) features that can differentiate between two hidden text categories.

You must reason purely from the textual content itself. Focus on OBSERVABLE, SURFACE-LEVEL features (specific words, phrases, grammatical patterns, sentence structure, length) rather than vague semantic labels. Each feature must be clearly answerable Yes or No for any new text.

I am providing you with example texts from two unknown categories.

Your task is to:
1. Examine all texts carefully and propose key binary features that systematically vary between the two categories.
2. Prefer features that are specific and observable (e.g. "starts with a question word", "contains a dollar amount", "uses past tense verbs") over abstract ones (e.g. "has formal tone").
3. Avoid features that are true (or false) for ALL texts — they must actually separate the two groups.
4. Express each feature as a short, clear attribute name answerable with Yes or No.
5. For each feature, provide a brief explanation of why it could be discriminative.
6. Provide at least 10 distinct features.

Output format:
{
  "proposed_features": [
    {
      "feature": "feature_name_1",
      "description": "short explanation why it may separate the two hidden groups",
      "possible_values": ["Yes", "No"]
    },
    {
      "feature": "feature_name_2",
      "description": "short explanation",
      "possible_values": ["Yes", "No"]
    }
  ]
}
